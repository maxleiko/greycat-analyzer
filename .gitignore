/target
/examples
/TODO.md

# GreyCat — runtime install destinations
/project.gcl
/gcdata
/files
/lib
/webroot
/bin

# wasm-pack output (rebuilt via `playground/scripts/build-wasm.sh`)
/crates/wasm/pkg

# Build artifacts inside the playground (vite scaffold's own .gitignore
# covers node_modules + dist; this catches any global slipups).
/playground/node_modules
/playground/dist

# Workspace TS packages — wasm/ holds wasm-pack output, dist/ holds
# tsdown output, node_modules/ holds pnpm-linked deps. Reproducible
# from source via `pnpm wasm` + `pnpm build`.
/packages/*/wasm
/packages/*/dist
/packages/*/node_modules
/node_modules

# Claude
/.claude/worktrees/
